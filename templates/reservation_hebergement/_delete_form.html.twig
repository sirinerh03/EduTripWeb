<form method="post" action="{{ path('app_reservation_hebergement_delete', {'id_reservationh': reservation_hebergement.idReservationh}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_hebergement.idReservationh) }}">
    <button class="btn">Delete</button>
</form>
