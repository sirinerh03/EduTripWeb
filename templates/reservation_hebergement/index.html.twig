{% extends 'base.html.twig' %}

{% block title %}ReservationHebergement index{% endblock %}

{% block body %}
    <h1>ReservationHebergement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_reservationh</th>
                <th>Hébergement</th>
                <th>Date_d</th>
                <th>Date_f</th>
                <th>Commentaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservation_hebergement in reservation_hebergements %}
            <tr>
                <td>{{ reservation_hebergement.idReservationh }}</td>
                <td>
                    {% if reservation_hebergement.hebergement %}
                        {{ reservation_hebergement.hebergement.nomh }}
                    {% else %}
                        N/A
                    {% endif %}
                </td>
                <td>{{ reservation_hebergement.dateD ? reservation_hebergement.dateD|date('Y-m-d') : '' }}</td>
                <td>{{ reservation_hebergement.dateF ? reservation_hebergement.dateF|date('Y-m-d') : '' }}</td>
                <td>{{ reservation_hebergement.status }}</td>
                <td>
                    <div style="display: flex; gap: 5px;">
                        <a href="{{ path('app_reservation_hebergement_show', {'id_reservationh': reservation_hebergement.idReservationh}) }}" 
                           class="btn" style="background-color: white; color: #06BBCC; border: 2px solid white;">
                            details
                        </a>
                        <a href="{{ path('app_reservation_hebergement_edit', {'id_reservationh': reservation_hebergement.idReservationh}) }}" 
                           class="btn" style="background-color: white; color: #06BBCC; border: 2px solid white;">
                            modifier
                        </a>
                        <form method="post" action="{{ path('app_reservation_hebergement_delete', {'id_reservationh': reservation_hebergement.idReservationh}) }}" 
                              onsubmit="return confirm('Are you sure you want to delete this item?');" style="display: inline;">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_hebergement.idReservationh) }}">
                            <button class="btn" style="background-color: white; color: #06BBCC; border: 2px solid white;">supprimer</button>
                        </form>
                    </div>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_hebergement_index') }}" class="btn" style="background-color: white; color: #06BBCC; border: 2px solid white;">
        faire une nouvelle reservation
    </a>
{% endblock %}