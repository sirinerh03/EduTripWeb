{% extends 'base.html.twig' %}

{% block title %}University{% endblock %}

{% block body %}
    <h1>University</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdUniversity</th>
                <td>{{ university.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ university.nom }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ university.ville }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ university.email }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ university.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_university_index') }}">back to list</a>

    <a href="{{ path('app_university_edit', {'idUniversity': university.id}) }}">edit</a>

    {{ include('university/_delete_form.html.twig') }}
{% endblock %}
