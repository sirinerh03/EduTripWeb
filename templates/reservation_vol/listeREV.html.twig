{% extends 'base.html.twig' %}

{% block title %}Réserver un vol{% endblock %}

{% block content %}
<div class="container mt-5">
    <h1 class="mb-4">Liste des vols disponibles ✈️</h1>

    {% if vols is not empty %}
        {% for vol in vols %}
            <div class="card mb-3 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">
                        ✈️ {{ vol.aeroportDepart }} → {{ vol.aeroportArrivee }}
                    </h5>
                    <p class="card-text">
                        <strong>Numéro de vol :</strong> {{ vol.numVol }}<br>
                        <strong>Date départ :</strong> {{ vol.dateDepart ? vol.dateDepart|date('d/m/Y H:i') : 'Non défini' }}<br>
                        <strong>Date arrivée :</strong> {{ vol.dateArrivee ? vol.dateArrivee|date('d/m/Y H:i') : 'Non défini' }}<br>
                        <strong>Places disponibles :</strong> {{ vol.placesDispo }}<br>
                        <strong>Prix :</strong> {{ vol.prixVol }} TND
                    </p>

                    <a href="{{ path('reservation_vol_form', {'idVol': vol.id_vol}) }}" class="btn btn-success btn-sm">
                        📝 Réserver
                    </a>
                </div>
            </div>
        {% endfor %}
    {% else %}
        <div class="alert alert-info">Aucun vol trouvé pour le moment.</div>
    {% endif %}
</div>
{% endblock %}
