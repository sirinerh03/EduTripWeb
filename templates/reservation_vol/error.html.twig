{# templates/bundles/TwigBundle/Exception/error.html.twig (ou autre nom personnalisé) #}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Erreur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8d7da;
            color: #721c24;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
            font-family: Arial, sans-serif;
        }
        .error-box {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            max-width: 600px;
        }
        .error-icon {
            font-size: 50px;
            color: #dc3545;
        }
    </style>
</head>
<body>
    <div class="error-box">
        <div class="error-icon">🚫</div>
        <h1>Une erreur est survenue</h1>
        <p>{{ message|default('Une erreur inattendue est survenue.') }}</p>
        <a href="{{ path('app_home') }}" class="btn btn-danger mt-3">Retour à l'accueil</a>
    </div>
</body>
</html>
