{% for post in posts %}
    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">{{ post.titre }}</h5>
            <p class="card-text">{{ post.contenu }}</p>
            <span class="badge bg-secondary">{{ post.categorie }}</span>
        </div>
    </div>
{% else %}
    <p>Aucun post trouvé.</p>
{% endfor %}
