<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Votre réduction EduTrip</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            color: #333;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 5px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #6a11cb;
            background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
            color: white;
            padding: 20px;
            border-radius: 5px;
        }
        .header h1 {
            font-size: 28px;
            margin-bottom: 10px;
        }
        .content {
            padding: 20px;
        }
        .reward-value {
            font-size: 48px;
            font-weight: bold;
            color: #6a11cb;
            text-align: center;
            margin: 30px 0;
        }
        .info {
            margin-bottom: 20px;
        }
        .info p {
            margin: 5px 0;
        }
        .footer {
            margin-top: 50px;
            text-align: center;
            font-size: 12px;
            color: #777;
        }
        .qr-code {
            text-align: center;
            margin: 30px 0;
        }
        .qr-code img {
            width: 150px;
            height: 150px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Félicitations !</h1>
            <p>Merci pour votre avis sur EduTrip</p>
        </div>

        <div class="content">
            <h2>Vous avez gagné</h2>

            <div class="reward-value">
                {{ reward.percentage }}% de réduction !
            </div>

            <div class="info">
                <p><strong>Description :</strong> {{ reward.description }}</p>
                <p><strong>Date d'obtention :</strong> {{ date|date('d/m/Y à H:i') }}</p>
                <p><strong>Bénéficiaire :</strong> {{ user.prenom }} {{ user.nom }}</p>
                <p><strong>Email :</strong> {{ user.mail }}</p>
                <p><strong>Code de réduction :</strong> EDUTRIP-{{ reward.id }}-{{ reward.percentage }}</p>
            </div>

            <div class="conditions">
                <h3>Conditions d'utilisation</h3>
                <ul>
                    <li>Cette réduction est valable pour votre prochain voyage éducatif réservé sur EduTrip.</li>
                    <li>La réduction s'applique sur le montant total de votre réservation.</li>
                    <li>Offre non cumulable avec d'autres promotions en cours.</li>
                    <li>Validité : 12 mois à partir de la date d'obtention.</li>
                </ul>
            </div>

            <div class="code-info" style="text-align: center; margin: 30px 0; padding: 15px; background-color: #f8f9fa; border-radius: 5px;">
                <p style="font-weight: bold; font-size: 18px; margin-bottom: 5px;">
                    Code de réduction
                </p>
                <p style="font-size: 24px; color: #6a11cb; margin: 0;">
                    EDUTRIP-{{ reward.id }}-{{ reward.percentage }}
                </p>
            </div>
        </div>

        <div class="footer">
            <p>Document généré le {{ date|date('d/m/Y à H:i') }}</p>
            <p>EduTrip - Voyages éducatifs - www.edutrip.fr</p>
        </div>
    </div>
</body>
</html>
