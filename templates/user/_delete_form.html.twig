<form method="post"
      action="{{ path('app_user_delete', {'id_user': user.idUser}) }}"
      onsubmit="return confirm('Supprimer cet utilisateur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete'~user.idUser) }}">
    <button class="btn">Supprimer</button>
</form>
